import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  wrapper: {
    height: 180,
    margin: 10,
    padding: 10,
    borderWidth: 1,
    borderColor: '#34495e'
  }
});

export default styles;
