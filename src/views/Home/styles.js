import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  wrapper: {
    width: 60,
    height: 60,
    padding: 10,
    borderWidth: 1,
    borderColor: '#34495e'
  }
});

export default styles;
